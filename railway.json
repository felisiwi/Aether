{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "git submodule update --init --recursive && npm ci && npm run build:server"
  },
  "deploy": {
    "startCommand": "npm run start"
  }
}
