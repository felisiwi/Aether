# HeldKeys

**Version**: v1.0.0  
**File**: `HeldKeys.1.0.0.tsx`

Three-line readout: notes row (` · ` separated), chord symbol, key name. **Display-only** — no `tabIndex`, button role, or click handlers; parents drive state.
