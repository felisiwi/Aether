# InstrumentInterface

**Version**: v1.0.0  
**File**: `InstrumentInterface.1.0.0.tsx`

Octave strip (`OctaveSection`) plus one octave of **PianoKey** v1.5.0 keys (`keyVariant` maps to `PianoKey` `variant`). Optional KeyNote-style rows when `showNoteHints` is true.
