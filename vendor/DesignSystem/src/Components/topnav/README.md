# TopNav

**Version**: v1.0.0  
**File**: `TopNav.1.0.0.tsx`

Centred **Aether** **Logo** (`../logo/Logo.1.0.2.tsx`; `colour` follows light/dark mode), optional back control, **ThemeWheel** with `variant="two"` (light/dark only).
