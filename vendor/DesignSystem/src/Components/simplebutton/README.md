# SimpleButton

**Version**: v1.0.0  
**File**: `SimpleButton.1.0.0.tsx`

Small action button using semantic button surfaces (`buttonSurfaceSmallbutton*`), strokes, and text tokens. Hover and pressed states are visual only (handled internally); `disabled` maps to disabled tokens.
