# SoundWaveController

**Version**: v1.0.0  
**File**: `SoundWaveController.1.0.0.tsx`

Waveform picker (sine, triangle, sawtooth, square) plus a circular toggle for oscilloscope visibility. **Not** `WaveformSelector` — separate component for the instrument strip.
